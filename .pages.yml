media:
  input: static/img
  output: /img
content:
  - name: blog
    label: Blog
    path: content/blog
    type: collection
    format: toml-frontmatter
    exclude: ["_index.md"]
    filename: "{year}-{month}-{day}-{primary}.md"
    fields:
      - name: title
        type: string
      - name: body
        type: rich-text
    view: 
      fields: [ title, date ]
      sort: [ date, title ]
      primary: title
      default:
        search: ''
        sort: date
        order: desc