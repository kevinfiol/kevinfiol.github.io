{
  "scripts": {
    "build": "node clean.js && eleventy --input=./src && node copy.js",
    "serve": "eleventy --serve",
    "clean": "node clean.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^3.0.1",
    "del": "^5.1.0",
    "fs-extra": "^8.1.0"
  }
}
